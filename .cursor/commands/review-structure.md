# review-structure

`app/`, `components/`, `lib/` 구조를 **읽기 전용**으로 리뷰한다.

## 에이전트 지시

1. App Router 트리, 공통 컴포넌트, `lib/` 역할을 요약한다
2. 네비게이션·콘텐츠 소스(`lib/` 등) 연결을 설명한다
3. 레거시 루트 HTML은 **참고만**; 실사용 기준은 Next라고 명시한다
4. 개선 아이디어는 `PROPOSAL_INBOX.md`로 보낼지 제안할 수 있다
5. **파일 내용을 변경하지 않는다** (이 command는 분석 전용)
